darkilliant_import:
    imports:
        # ./console process:run 
        import_product_easy:
            source: 'file://@[context][file]'
            entity_class: 'App\Entity\Product'
            mapping:
                label: '@[data][name]'
